#@ load("@ytt:data", "data")
---
fullName:
  name: block-latest
  workspaceName: #@ data.values.workspace_name
spec:
  input: {}
  namespaceSelector:
    matchExpressions:
    - key: block-latest
      operator: In
      values:
      - "true"
  recipe: block-latest-tag
  recipeVersion: v1
  type: image-policy